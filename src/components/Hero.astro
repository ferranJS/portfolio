---
import { useTranslations } from "../i18n/utils"
import { languages } from "../i18n/languages"

export function getStaticPaths() {
  const paths = []
  for (const language of Object.keys(languages)) {
    paths.push({ params: { language } })
  }
  return paths
}

const lang = Astro.params.language
const t = useTranslations(lang)
---

<style>
  h1.fade-in-up {
    animation-delay: 0.2s;
  }
  h2.fade-in-up {
    animation-delay: 0.6s;
  }
  div.fade-in {
    animation-delay: 1.3s;
  }
  .scroll-fade-out {
    animation: fade-out ease-out both;
    animation-timeline: view();
    animation-range: cover 58% cover 90%;
  }
  .scroll-fade-out.right {
    animation: fade-out-right ease-in both;
    animation-timeline: view();
    animation-range: cover 60% cover 99%;
  }
  .scroll-fade-out.left {
    animation: fade-out-left ease-in both;
    animation-timeline: view();
    animation-range: cover 65% cover 99%;
  }
</style>
<section class="flex h-screen items-center" id="hero">
  <div class="mt-10 overflow-clip px-7 pr-10 text-[1.2rem] sm:px-24 xl:px-48">
    <h1 class="fade-in-up mb-2 font-primary text-6xl font-medium sm:text-[5rem]">
      <strong class="scroll-fade-out left block">Ferran Ramírez</strong>
    </h1>
    <h2 class="fade-in-up mb-6 font-primary text-4xl leading-[1.15] sm:text-5xl">
      <strong class="scroll-fade-out right block">
        <span class="indigo-wavy !decoration-[3px] !underline-offset-[5px]"> full stack</span> developer
      </strong>
    </h2>
    <div class="fade-in max-w-3xl">
      <div class="scroll-fade-out [&>p]:mb-2">
        <p>
          {
            lang == "es" ? (
              <>
                <strong>
                  <span class="indigo-wavy !decoration-[1.3px]">Ingeniero</span> de Software
                </strong>{" "}
                con 2 años de experiencia como desarrollador
                <span class="indigo-wavy !decoration-[1.3px]">Full Stack</span>, experimentado en
              </>
            ) : lang == "ca" ? (
              <>
                <strong>
                  <span class="indigo-wavy !decoration-[1.3px]">Enginyer</span> de Software
                </strong>{" "}
                amb 2 anys d'experiència com a desenvolupador
                <span class="indigo-wavy !decoration-[1.3px]">Full Stack</span>, experimentat en
              </>
            ) : lang == "ru" ? (
              <>
                <strong>
                  <span class="indigo-wavy !decoration-[1.3px]">Инженер</span>-программист
                </strong>{" "}
                с двумя годами опыта работы в качестве
                <span class="indigo-wavy !decoration-[1.3px]">Full Stack</span> разработчика, имеющего опыт с
              </>
            ) : (
              <>
                <strong>
                  Software <span class="indigo-wavy !decoration-[1.3px]">Engineer</span>
                </strong>{" "}
                with 2 years of experience as a <span class="indigo-wavy !decoration-[1.3px]">Full Stack</span>{" "}
                developer, experienced in
              </>
            )
          }
          <span class="text-red-500">Angular</span>,
          <span
            class="text-orange-500 dark:text-orange-300"
            >Astro</span
          >,
          <span
            class="text-sky-400 dark:text-sky-300"
            >Tailwind</span
          >, Vercel, Firebase, {t("and")} Capacitor.
        </p>
        <p>
          <Fragment set:html={t("phrase1")} />
          {/* <Fragment set:html={t("phrase2")} /> */}
        </p>
        <p>
          <Fragment set:html={t("phrase3")} />
        </p>
      </div>
    </div>
  </div>
</section>
